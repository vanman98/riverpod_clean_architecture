# 0.1.0+1

- Initial release
- Generate Clean Architecture feature structure with:
  - Data layer (datasources, models, repository implementation)
  - Domain layer (entities, repository, use cases)
  - Presentation layer (state management, UI pages)
  - Dependency injection providers
